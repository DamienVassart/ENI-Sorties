{% extends 'base.html.twig' %}

{% block body %}
    <div id="blocRecherche">
        {{ form_start(searchForm) }}
        {{ form_widget(searchForm) }}
        {{ form_end(searchForm) }}
    </div>

    <div>
        {% for campus in listeCampus %}
{#            <ul>#}
{#                <li>#}
                    <div class="campus">
                        {{ campus.nom }}
                        <a href="{{ path('admin_campus_edit', {'id': campus.id}) }}"><button>Modifier</button></a>
                        <a href="{{ path('admin_campus_delete', {'id': campus.id}) }}"><button>Supprimer</button></a>
                    </div>
{#                </li>#}
{#            </ul>#}
            <br>
        {% endfor %}
    </div>

    <div id="container">
        {{ form_start(campusForm) }}
        {{ form_widget(campusForm) }}
        <button type="submit" class="btn-secondary btn-block">Ajouter</button>
        {{ form_end(campusForm) }}
    </div>
    <br>

{% endblock %}

{% block title %}
    Gestion des campus
{% endblock %}
